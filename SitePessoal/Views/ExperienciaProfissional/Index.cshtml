@model IEnumerable<SitePessoal.Models.ExperienciaProfissional>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trabalho)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duracao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Funcao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Trabalho)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duracao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funcao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ExperienciaProfissionalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ExperienciaProfissionalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ExperienciaProfissionalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
