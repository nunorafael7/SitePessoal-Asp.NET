@model IEnumerable<SitePessoal.Models.Escola>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar nova entrada na Tabela</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeEscola)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataFim)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeEscola)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataFim)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EscolaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.EscolaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.EscolaId">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
